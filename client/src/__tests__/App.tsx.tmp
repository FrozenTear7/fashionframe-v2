import * as React from 'react';
import { render, screen } from '@testing-library/react';
import App from '../App';

test('renders fashionframe', () => {
  render(<App />);
  const linkElement = screen.getByText(/Fashionframe/i);
  expect(linkElement).toBeInTheDocument();
});
